"""
===============================================================================
  UWB RTLS Studio — Helpers
===============================================================================
  File        : utils/helpers.py
  Description : Utility functions dùng chung trong app.

  Functions:
    - format_mac(mac_bytes) → "AA:BB:CC:DD:EE:FF"
    - format_version(version_t) → "1.2.1 (build 45)"
    - format_timestamp(epoch_ms) → "2026-05-30 12:30:00"
    - format_elapsed(seconds) → "01:23:45"
    - bytes_to_hex(data) → "0x00 0x01 0x02"
    - device_type_str(enum_val) → "TAG" / "ANCHOR" / "GATEWAY"
    - rssi_to_quality(rssi_dbm) → "Excellent" / "Good" / "Fair" / "Poor"
    - clamp(value, min_val, max_val) → clamped value
===============================================================================
"""
pass
